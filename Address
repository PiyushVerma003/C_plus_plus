#include<iostream>

using namespace std;

int main() {
    int a = 564;
    int* b = &a;
    cout << "Address of a: " << b << endl; // This will print the address
    cout << "Value of a: " << *b << endl; // This will print the value of a
}
